/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["34.118.123.231", "api.stage.goods2load.com", "localhost", "production-dubainight.s3.me-south-1.amazonaws.com"],
  },
};

export default nextConfig;
